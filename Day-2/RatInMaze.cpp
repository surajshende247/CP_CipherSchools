#include<bits/stdc++.h>
using namespace std;

bool isSafe(vector<vector<int>>& grid)

bool ratInMaze(vector<vector<int>>& grid, vector<vector<int>> &visited, int x, int y, int R, int C){
    if(x == R-1 && y == C-1 && grid[x][y] == 0){
        visited[x][y] = true;
    }
}

bool ratInMaze(vector<vector<int>> grid){

}

int main(){
    return 0;
}